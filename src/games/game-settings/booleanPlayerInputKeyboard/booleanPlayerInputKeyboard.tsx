import PlayerInput from '../PlayerInput'

const booleanPlayerInputKeyboard: PlayerInput = {
  renderEdit: () => {
    return (
      <>Boolean input editor</>
    )
  }
}

export default booleanPlayerInputKeyboard
