import { CSSObject } from '@emotion/css'

const pausedStyles: CSSObject = {
  position: 'absolute',
  width: '100%',
  height: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center'
}

export default pausedStyles
