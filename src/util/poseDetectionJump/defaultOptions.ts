import Options from './Options'

const defaultOptions: Options = {
  recordLastMs: 200,
  deadZoneJump: 5,
  deadZoneSpeed: 0.1,
  maxLandingDiff: 30,
  maxJumpDuration: 1000
}

export default defaultOptions
