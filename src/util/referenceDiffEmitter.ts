// import { create, ReferenceManagerEmitter } from './ReferenceManager/Emitter'

/**
 * Only re-emits if value is different than previous
 */
// eslint-disable-next-line max-len
// const referenceDiffEmitter = <T>(emitter: ReferenceManagerEmitter<T>): ReferenceManagerEmitter<T> => {
//   const diffEmitter = new Set()
//   return create(diffEmitter, {
//     load: () => {

//     },
//     unload: listener => {

//     }
//   })
// }
